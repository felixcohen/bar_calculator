%h1 Overview

= render :partial => 'report'

%h2 Reports
%table
  %tr
    %th Day
    %th Sales
    %th 
  -@days.each do |day|
    %tr
      %td
        =day[0]
      %td
        =day[1]
      %td
        - report_url = '/report/'+day[0].strftime("%d-%m-%Y")
        = link_to('Report', report_url)
%br


= link_to 'New Drink', new_drink_path
