= form_for @sale do |f|
  - if @sale.errors.any?
    #error_explanation
      %h2= "#{pluralize(@sale.errors.count, "error")} prohibited this sale from being saved:"
      %ul
        - @sale.errors.full_messages.each do |msg|
          %li= msg
- @products.each do |product|
  = button_to product.name, {:controller => "sales", :action => "create", :method => "post", product_id: product.id}, :class => "btn btn-primary btn-lg" , :remote=>true
= link_to "Add Discount", new_discount_path
